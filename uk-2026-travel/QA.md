# 重構驗收紀錄 · 2026/09/05

版本：`rebuild-20260905-1`。

- 17 個 HTML 頁面（首頁、D1–D8、飯店總覽與三間飯店、攻略、飲食卡、清單、404）全部載入。
- 390×844 與 430×932 的內容 viewport 實測；Chrome 在同源 iframe 中渲染，桌面捲軸寬度已補正。這是手機尺寸排版測試，**不代表 Safari／iPhone 真機測試**；目前瀏覽器環境僅提供 Chrome。
- 各頁收合與展開的攻略、白天／夜間／D7 機場模式，`scrollWidth === clientWidth`，沒有橫向溢出。
- 主要按鈕、地圖連結、日期導航及時間控制項高度至少 44px。
- D2–D7 每頁恰好一個自由時間區、一份表單、一份共用 JS；Tab 切換保留各自輸入，不插入第二套計算器。
- 手機實際操作：D2 修改時間先失效、重算 30 分；23:30→翌日00:30 扣10分為50分；選同日歸零；切回白天仍保留輸入。
- D6 夜間巴士預設時段顯示「來不及」；提早到19:00可算出20:30班，並提醒確認票券。
- D7 三個 Tab 正常；機場18:30–20:15扣20分為85分；20:00才開始則為0分。
- 清單勾選後重載仍保留，測試勾選已取消。
- `tools/verify_site.py`：342 個站內連結／錨點、43 個不重複 Google Maps URL 格式、17 套完整六區攻略、25 段遊覽車＋1 段步行、關鍵正式行程與移除舊引用全部通過。
- `tools/test_time.js`：17 個邊界情境通過，涵蓋跨午夜、過期、無效輸入、重複緩衝風險、店家關門、巴士最晚班次及機場截止。
- HTML、CSS、JS 可直接從 GitHub Pages 子目錄使用相對路徑，初次只請求當頁 HTML、共用 CSS／JS；首頁圖片在下方延遲載入。

43 個 Google Maps 外連已逐一檢查，初次部分逾時，一次重查後全部收到 HTTP 200。HTTP 回應只證明網址可連線，不能保證商店當日營業、商品庫存或即時道路狀況。
